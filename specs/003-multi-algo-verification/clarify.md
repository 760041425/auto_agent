# 003 澄清记录

| ID | 问题 | 结论或假设 | 状态 | 是否阻塞 | 决策人 |
| --- | --- | --- | --- | --- | --- |
| CL-003-01 | 当前五算法是否均通过 Web/API 运行真实实现？ | 否。注册值与分派条件不一致，benchmark 直调成功不能证明 API 路径正确。 | 已确认 | 否，Phase A 可直接修复 | 代码证据 |
| CL-003-02 | 当前 `verify_err_m` 是否为独立真值误差？ | 否。现有实现包括同一 NPY 自比较、同一匹配对应的内部一致性和点到射线残差，均不是独立真值。 | 已确认 | 阻塞精度结论 | 代码证据 |
| CL-003-03 | 低于 `min_inliers` 是否仍算成功？ | 保留 `success=true` 表示求解器有解，同时增加 `reliable=false`；UI 展示为低可信。 | Phase A 已采用 | 否 | 本轮修复授权 |
| CL-003-04 | 算法无解是否令整个任务失败？ | 任务可完成但该算法结果 `success=false`；只有调度、依赖或持久化异常才使任务进入 `failed`。 | Phase A 已采用 | 否 | 本轮修复授权 |
| CL-003-05 | 绝对精度真值来自哪里？ | 候选：已知相机位姿、人工控制点、独立测量轨迹。必须与被评算法输入解耦。 | TODO（暂时遗留） | 是，阻塞 Phase B 推荐 | 用户/算法负责人 |
| CL-003-06 | 目标运行硬件和延迟门槛是什么？ | 当前报告混用 MPS/CPU 可能性，暂不承诺 `<10s`。需要指定设备、预热和统计口径。 | TODO（暂时遗留） | 是，阻塞性能验收 | 用户/算法负责人 |
| CL-003-07 | benchmark 最小样本量和场景分层是什么？ | 建议先建立固定 holdout manifest，覆盖视角、光照、距离和纹理层级；样本量由可用真值决定。 | TODO（暂时遗留） | 是，阻塞最终推荐 | 用户/算法负责人 |
| CL-003-08 | 生成报告是否提交 Git？ | 原始 HTML/JSON 放 `reports/generated/` 并忽略；只提交小型基线摘要、manifest 和验收报告。 | Phase A 已采用 | 否 | 本轮修复授权 |
| CL-003-09 | 是否保持旧 API 字段？ | 一个发布周期内提供兼容适配，同时新增统一嵌套契约；前端迁移后再删除旧字段。 | Phase A 已采用 | 否 | 本轮修复授权 |
| CL-003-10 | 本轮是否优化算法本身？ | 否。先修可信运行与可信测量；准确率优化另开规格。 | 已确认 | 否 | 当前范围 |
| CL-003-11 | 本地 H/NPY 坐标差用什么门槛决定可信？ | 用户明确确认：中位坐标差必须严格 `<0.3m`；等于或超过 0.3m 均判为不准。`coordinate_threshold_m` 默认 0.3。 | 已确认 | 否 | 用户明确指示 |
| CL-003-12 | 一致性判据是否必须包含 Z 维度？ | 是。单应矩阵 H 映射到 SLAM 地面平面（Z=0），NPY 含真实高度，应比较三维欧氏距离。修复前 XY-only 会导致高度异常位姿被错误判为可靠。 | 已确认（2026-08-03） | 否 | 代码证据 + task #249 验证 |
| CL-003-13 | 精化步骤是否必须复用初始匹配器？ | 是。SALAD+RoMa 初始用 TinyRoMa，精化必须复用 TinyRoMa 而非固定 LightGlue。其他算法保持 LightGlue 作为默认回退。 | 已确认（2026-08-03） | 否 | task #249 日志证据（LightGlue 1 对 vs TinyRoMa 273 对） |

## 已确认事实

- 修复前快速测试为 `20 passed, 4 deselected`；Phase A 修复后为 `46 passed, 4 deselected`；BUG-003-04/05 修复后为 `85 passed, 4 deselected`。
- Spec 003 原先缺少七份规格资产，规格和漂移门禁失败。
- `services/matcher/__init__.py` 的 `Path` 缺失、V2 的 `best_pose` 未定义和 NPY 同像素自比较已在 Phase A 修复。
- 当前报告只包含少量本地查询影像，且多个 `final` 文件的指标不一致。
- BUG-003-04：task #249 单点查询坐标差 0.968m（含 Z=-0.656m），修复前 XY-only 为 0.710m。
- BUG-003-05：task #249 精化从 LightGlue 1 对失败 → TinyRoMa 273 对成功。

## 工作假设

- Phase A 不依赖新增真值数据，可以先恢复正确分派、契约和运行稳定性。
- TODO：Phase B 在用户提供或确认真值来源后继续；此前所有算法推荐均为实验观察。
- 不改变现有数据库 schema，统一结果继续存入 `TaskModel.result_json`。
- 当前 `las/points3D.txt` 明确声明 0 个点，COLMAP points 集成测试因此保持失败；不得伪造地图点作为通过证据。
