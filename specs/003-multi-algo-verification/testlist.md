# 003 测试清单

| 状态 | TL-ID | 映射 AC | 层级 | 场景与期望 |
| --- | --- | --- | --- | --- |
| [x] | **TL-003-01** | AC-003-01 | 单元/契约 | 参数化五个公开算法 ID，以 spy runner 证明每个 ID 精确调用对应实现且参数透传 |
| [x] | **TL-003-02** | AC-003-01 | API | 未知算法、空算法列表、畸形内参分别返回可诊断 422，不创建后台任务 |
| [x] | **TL-003-03** | AC-003-02 | 契约 | 成功结果序列化后保留 pose、quality、validations、artifacts、timings，旧结果可兼容读取 |
| [ ] | **TL-003-04** | AC-003-02,AC-003-03 | 契约 | 一个算法无解不吞掉其他算法结果；内部异常包含 algorithm_id/error code，任务结果可读取 |
| [x] | **TL-003-05** | AC-003-03 | 单元 | 无坐标差判据的旧结果继续兼容 `min_inliers`；V2 由 TL-003-29 覆盖并以坐标差覆盖点数判定 |
| [x] | **TL-003-06** | AC-003-07 | 单元 | matcher 路径回退和 tile index 读取不出现 `Path` NameError |
| [ ] | **TL-003-07** | AC-003-07 | 单元 | V2 runner 保存实际最佳候选，验证路径不引用未定义变量，失败时返回结构化原因 |
| [x] | **TL-003-08** | AC-003-04 | 单元 | 无独立 reference 时绝对精度状态为 `not_available`，数值不是 `0.0` |
| [x] | **TL-003-09** | AC-003-04 | 单元 | 投影一致性使用两个不同来源/路径计算；故意偏移匹配点时误差显著增大 |
| [x] | **TL-003-10** | AC-003-04,AC-003-05 | 单元 | 合成已知位姿数据得到可预测的平移误差与旋转误差，并与重投影误差分开输出 |
| [x] | **TL-003-11** | AC-003-05,AC-003-06 | 集成 | benchmark manifest 固定数据、配置、seed、设备和 commit；同输入复跑生成不同 run_id 但相同配置哈希 |
| [x] | **TL-003-12** | AC-003-06 | 验收 | 无真值或样本门槛未满足时报告不出现“最终推荐”，而显示证据缺口 |
| [x] | **TL-003-13** | AC-003-07 | API/UI | API 返回投影/LAS/真值验证和错误字段，前端能区分成功、低可信、无解和内部错误 |
| [x] | **TL-003-14** | AC-003-08 | 单元 | 重复初始化或重复导入日志配置不增加 handler，日志包含 task_id 与 algorithm_id |
| [x] | **TL-003-15** | AC-003-09 | 系统 | 临时工作目录中不存在 logs/projections/reports 时应用可启动、创建目录并通过 health check |
| [x] | **TL-003-16** | AC-003-09 | 集成 | 原比较任务使用缺失或目录型 tile/coord path 时返回领域失败而非未定义符号 |
| [x] | **TL-003-17** | AC-003-10 | 门禁 | `validate-specs`、`drift-check`、快速测试和变更范围 Ruff 检查通过 |
| [ ] TODO | **TL-003-18** | AC-003-05,AC-003-10 | 真实数据 | 在批准的 holdout 真值集上运行全部算法，输出成功率、可靠率、平移/旋转误差分布和延迟分布 |
| [x] | **TL-003-19** | AC-003-11 | 单元/文件 | V2 最终 artifact 生成器写出查询图、最终位姿投影图和双图对比图，且文件非空；task #212 同输入真实 LoFTR runner 复跑生成三图并保持 22 内点及相同位姿 |
| [x] | **TL-003-20** | AC-003-02,AC-003-11 | 契约/API | 统一结果和 API URL 适配保留 `query_image`、`reprojection_image`、`comparison_image` |
| [x] | **TL-003-21** | AC-003-11 | UI/系统 | 静态测试覆盖最新结果和即时轮询共用渲染函数；浏览器确认历史缺失 artifact 显示明确原因；task #217 结果卡显示查询图与最终投影，三张 PNG 均完成加载 |
| [x] | **TL-003-22** | AC-003-04,AC-003-12 | 单元 | 同源 NPY 验证返回米制 `not_available/None`，只保留可观察的 homography 像素拟合残差 |
| [x] | **TL-003-23** | AC-003-12 | UI/契约 | 页面明确区分“2D 几何拟合诊断（非 Benchmark）”与“独立真值 Benchmark”，无真值时显示未执行 |
| [x] | **TL-003-24** | AC-003-04,AC-003-12 | 静态/报告 | HTML 与 CLI 两条验证脚本不再输出同源 NPY 米制误差，统一使用像素残差 |
| [x] | **TL-003-25** | AC-003-13 | 单元 | 最终位姿生成查询图空间 XYZ NPY（近点遮挡远点），2D–3D 内点拟合 H；本地查询保留 `pixel_to_slam`、`npy_point` 和差值，无效 NPY 像素不得伪造 0 米 |
| [x] | **TL-003-26** | AC-003-13 | API/契约 | 后端按 `task_id/result_index` 读取本地坐标产物且只接受 `[0,1]` 归一化点；源码不含 `SLAM_MAP_URL`、远程适配器或 camera 运行依赖 |
| [x] | **TL-003-27** | AC-003-13 | UI/系统 | 查询图绑定本地任务选点事件，页面分别展示 `H→SLAM XYZ`、`NPY XYZ`、坐标差及“非绝对精度”声明；task #223 浏览器点击返回真实坐标 |
| [x] | **TL-003-28** | AC-003-14,AC-003-16 | 单元 | 对最终 XYZ NPY 有效像素确定性采样，计算三维差（含 Z 分量）的 median/P95/max；仅中位差 `<0.3m` 通过，等于或超过门槛必须失败；XY 对齐但 Z 偏移时中位差必须反映 Z 偏差 |
| [x] | **TL-003-29** | AC-003-14 | 契约 | 即使 `inliers` 很高，只要坐标差未通过，统一结果 `reliable=false`；坐标差通过时才可为 true |
| [x] | **TL-003-30** | AC-003-14 | UI/系统 | 结果卡以“坐标差最终判定”展示中位差、P95、样本数和门槛；点数/相似度/2D 拟合/Benchmark 收入辅助诊断；task #225 浏览器显示低可信和 28.448m/3m |
| [x] | **TL-003-31** | AC-003-15 | 单元/真实数据 | 给定与当前 tile key 零交集的 245 条旧缓存，以及覆盖当前 key 的 V2 `multi` 缓存，原版选择兼容缓存并能返回候选；不得静默接受旧缓存后返回 0 候选 |
| [x] | **TL-003-32** | AC-003-01,AC-003-15 | 单元/真实数据 | `salad_roma` 的局部匹配适配器调用 TinyRoMa `match/sample` 并返回像素对应点；测试令 LightGlue 调用直接失败，防止算法标签与实际 runner 再次漂移 |
| [x] | **TL-003-33** | AC-003-14,AC-003-15 | 单元/真实数据 | 原版 SALAD+RoMa 成功结果生成查询图、最终位姿 XYZ NPY 与 H 上下文；即使 PnP 内点很高，坐标差未通过 `<0.3m` 时 `reliable=false` |
| [ ] TODO | **TL-003-34** | AC-003-17 | 单元 | 精化步骤根据 `matcher_type` 分派到对应匹配器（SALAD+RoMa → TinyRoMa）；默认无参数时回退 LightGlue 保持向后兼容 |
| [ ] TODO | **TL-003-35** | AC-003-17 | API/契约 | `/localize/refine` 端点从初始结果 algorithm_id 推导 matcher_type 传入精化函数；未知 matcher_type 返回结构化 422 |

## TDD 顺序

优先依次推进 `TL-003-01`、`TL-003-03`、`TL-003-06`、`TL-003-07`、`TL-003-08`；每次先确认测试因目标缺陷失败，再做最小修复。真实模型和大文件测试使用 `integration/system` 标记，不进入默认快速回路。
