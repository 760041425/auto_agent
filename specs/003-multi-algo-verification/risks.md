# 003 风险

| RISK-ID | 概率/影响 | 触发信号 | 缓解 | 回滚 |
| --- | --- | --- | --- | --- |
| **RISK-003-01** | 高/高 | API 显示某算法但调用另一个 runner | 单一注册表、参数化 spy 契约测试 | 回退到只开放已验证算法 ID |
| **RISK-003-02** | 高/高 | 内部一致性被误写成绝对精度 | 指标类型枚举、无真值返回 unavailable、报告显式数据来源 | 禁用推荐和精度列，保留原始诊断 |
| **RISK-003-03** | 高/高 | 真值与训练/地图输入同源导致数据泄漏 | 独立 holdout manifest、记录真值来源与隔离规则 | 废弃受污染运行，重建数据集 |
| **RISK-003-04** | 中/高 | 旧前端或历史任务无法解析新结果 | 只增字段、兼容适配、契约版本 | 保留旧 schema adapter 一个发布周期 |
| **RISK-003-05** | 中/中 | CPU/MPS、首次模型加载导致延迟不可比 | 固定设备、预热、重复运行、报告 P50/P95 | 报告只展示原始耗时，不作门槛判断 |
| **RISK-003-06** | 中/高 | 随机采样导致结果和推荐漂移 | 全链路 seed、确定性采样、保存 config hash | 禁止生成推荐，标记 run 不可复现 |
| **RISK-003-07** | 中/中 | 多算法并行/串行运行耗尽内存或阻塞 API | 任务边界、资源限制、超时与逐算法清理 | 限制同时启用算法数，回退串行后台执行 |
| **RISK-003-08** | 中/中 | 报告嵌入查询影像泄露数据或让 Git 膨胀 | generated 目录忽略、脱敏、只提交摘要 | 删除生成物并重新发布脱敏摘要 |
| **RISK-003-09** | 中/高 | 模型权重架构与加载器不一致 | 权重 manifest、架构检测契约、明确失败 | 回退到已验证权重和旧 loader |
| **RISK-003-10** | 低/高 | 删除 `.gitkeep` 后干净环境无法启动 | 启动创建目录 + clean-clone system test | 恢复占位文件并回退挂载改动 |
| **RISK-003-11** | 中/中 | 日志重复 handler 导致重复写入和文件句柄增长 | 幂等配置测试、集中 logger factory | 回退到单一 root handler |
| **RISK-003-12** | 高/中 | 两张样本导致过拟合式算法推荐 | 场景分层、独立 holdout、置信区间 | 删除“最终推荐”，只保留实验观察 |
| **RISK-003-13** | 高/中 | runner 成功但不生成前端依赖的视觉 artifact | 成功路径产物契约、文件测试、浏览器纵向验收 | artifact 失败显式降级，不影响位姿结果 |
| **RISK-003-14** | 高/高 | 同源 NPY 自比较被误认为 Benchmark 或米制精度 | 禁止同源米制字段，拟合/Benchmark 分栏，报告扩散测试 | 无独立真值时只显示像素诊断与证据缺口 |
| **RISK-003-15** | 中/高 | 最终位姿 XYZ NPY 稀疏、H 拟合失败或历史任务没有本地产物 | 近点 z-buffer splat、H 最少 4 内点、无效像素显式 unavailable、新任务自产产物 | 禁用该结果的选点但保留定位和视觉产物 |
| **RISK-003-16** | 中/高 | 单点差偶然命中或内点数掩盖整体坐标不一致 | 最多 256 点确定性采样、中位差主判据、P95 辅助观察、无指标即低可信 | 提高样本数或收紧可配置门槛，不回退点数判定 |
| **RISK-003-17** | 高/高 | tile 重建后描述子缓存仍可读取但 key 零交集，原版检索返回 0 候选 | 加载时校验当前有效 key；兼容 V2 `multi` 缓存回退；零交集回归测试 | 删除失配缓存并重建原版索引 |
| **RISK-003-18** | 中/高 | Z 维度修复后更多位姿被判为 `reliable=false`，用户误以为系统退化 | 明确文档说明这是正确行为（修复前是假阳性）；提供 Z 分量诊断字段 | 回退 XY-only 判据（不推荐） |
| **RISK-003-19** | 中/中 | TinyRoMa 在重投影图上的匹配质量未验证（训练域是 tile↔query） | 真实任务验证（task #249 已通过 273 对）；必要时调整 `sample_num` | 回退 LightGlue 并增加匹配数阈值 |
