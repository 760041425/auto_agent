# 005 决策记录

## DEC-005-01：前端格式化逻辑

- 状态：待实施
- 背景：API 返回格式已变更，前端需要适配
- 决定：优先显示 `difference_px`（新字段），回退到 `difference_m`（旧字段）
- 理由：
  1. 新字段是主要验证方式
  2. 旧字段作为回退兼容历史任务
- 后果：前端代码需要条件判断

## DEC-005-02：测试方式

- 状态：待实施
- 背景：前端是 JavaScript，项目无 JS 测试框架
- 决定：写纯 Python 测试，模拟 API 响应，验证格式化函数
- 理由：
  1. 格式化逻辑可独立测试
  2. 不需要浏览器环境
- 后果：测试覆盖格式化逻辑，不覆盖 DOM 渲染
