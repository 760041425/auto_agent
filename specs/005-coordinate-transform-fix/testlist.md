# 005 测试清单

| 状态 | TL-ID | 映射 AC | 层级 | 场景与期望 |
| --- | --- | --- | --- | --- |
| [ ] | **TL-005-01** | AC-005-01 | 单元 | API 返回 `difference_px=1.7`，格式化后包含 "重投影误差: 1.7 px" |
| [ ] | **TL-005-02** | AC-005-02 | 单元 | API 返回 `pixel_to_slam=null`，格式化后不包含 "不可用" |
| [ ] | **TL-005-03** | AC-005-03 | 单元 | API 返回旧格式 `difference_m=0.5`，格式化后包含 "坐标差: 0.5 m" |
| [ ] | **TL-005-04** | AC-005-04 | 单元 | 格式化后包含备注 "PnP位姿重投影NPY点到像素" |
| [ ] | **TL-005-05** | AC-005-01 | 单元 | API 返回完整新格式，格式化后包含 NPY XYZ 和重投影误差 |

## TDD 顺序

### 批次 P0：核心场景

1. 写 TL-005-01 → 跑 → 确认红 → 实现 → 确认绿
2. 写 TL-005-02 → 跑 → 确认红 → 实现 → 确认绿
3. 写 TL-005-03 → 跑 → 确认红 → 实现 → 确认绿
