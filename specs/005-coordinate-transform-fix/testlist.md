# 005 测试清单

| 状态 | TL-ID | 映射 AC | 层级 | 场景与期望 |
| --- | --- | --- | --- | --- |
| [x] | **TL-005-01** | AC-005-01 | 单元 | API 返回 `error_px=1.7`，格式化后包含 "重投影像素误差: 1.7 px" |
| [x] | **TL-005-02** | AC-005-02 | 单元 | API 不返回 `pixel_to_slam`（`slam_xy` 可为 null），格式化后不包含 "不可用" |
| [x] | **TL-005-03** | AC-005-03 | 单元 | API 返回 `error_m=0.5`，格式化后包含 "坐标误差: 0.500 m" |
| [x] | **TL-005-04** | AC-005-04 | 单元 | 格式化后包含备注 "PnP位姿+射线平面求交 vs NPY" |
| [x] | **TL-005-05** | AC-005-01 | 单元 | API 返回完整新格式（slam_xy/npy_xyz/error_m/error_px），格式化后包含 NPY XYZ 和重投影像素误差 |

## TDD 顺序

### 批次 P0：核心场景

1. 写 TL-005-01 → 跑 → 确认红 → 实现 → 确认绿
2. 写 TL-005-02 → 跑 → 确认红 → 实现 → 确认绿
3. 写 TL-005-03 → 跑 → 确认红 → 实现 → 确认绿
