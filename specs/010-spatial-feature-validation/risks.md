# 010 风险

| RISK-ID | 概率/影响 | 风险与触发信号 | 缓解 | 回滚 |
| --- | --- | --- | --- | --- |
| RISK-010-01 | 高/高 | tile 查询命中索引自身，成功率和精度虚高 | 强制 leave-one-out key 校验；报告分组 | 废弃受污染报告 |
| RISK-010-02 | 高/高 | MiDaS 相对深度被当作可直接反投影的 metric depth | 深度类型入契约；先做角误差门 | 保持法线不参与定位 |
| RISK-010-03 | 中/高 | 世界法线与相机法线坐标/轴约定混用 | `R_cw` 显式变换、单位化、无向/有向同时报告 | 关闭几何评分 |
| RISK-010-04 | 中/中 | 冷启动和产物生成掩盖匹配器真实耗时 | 分段计时、区分 cold/warm；pose-only 显式跳过非评分后处理 | 关闭 pose-only 即恢复完整产物 |
| RISK-010-05 | 中/高 | hloc/ACE0/SCR 引入依赖或权重过重 | hloc 最小子集先行；记录下载体积；>2GB 停止 | 不安装并保留研究路径 |
| RISK-010-06 | 高/高 | 用重投影/LAS 邻近性冒充绝对精度 | 只有 translation/rotation holdout 可进入准确性决策 | 决策标“证据不足” |
| RISK-010-07 | 中/中 | 1 张 leave-one-out 偶然性强 | n=1 只作契约烟测；决策门要求 8+2 | 不切默认路由 |
| RISK-010-08 | 已发生/高 | macOS pycolmap、Torch、可选提取器各自捆绑 libomp，进程原生 abort | 完整 pipeline 显式 skip；安全加载官方 SuperPoint/LightGlue 必需模块；禁止不安全环境变量 | 移除 hloc 前端适配，保持 LoFTR |
| RISK-010-09 | 高/中 | 当前 MoGe 3 依赖 Triton/FlexGEMM 且无 macOS wheel，直接安装会污染或阻断环境 | 固定 MoGe-2 `42acd8f`；安装前审计依赖；只用官方 ViT-S normal 权重 | 卸载可选包、删除模型缓存，不改生产路径 |
| RISK-010-10 | 中/高 | 预测法线虽有限但坐标系/朝向与地图法线不兼容，软评分会误伤正确对应 | 记录 OpenCV 相机系；统一 `R_cw`；用无向角总体中位数 20° 门槛 | 门槛失败即停止，不接入软评分 |
| RISK-010-11 | 已发生/高 | 已发布 `normal.npy` 与同一 raw XYZ 的直接中心差分法线不一致，模型比较参考不稳定 | 同时报告 stored/raw 两套诊断；下一切片先把参考误差稳定降至 10° 内 | 保持所有法线不参与定位 |
| RISK-010-12 | 已发生/高 | 按轴 min-max 平滑破坏旋转等变；中心差分跨无效孔洞产生假单位法线 | 移除按轴平滑；要求完整四邻域；合成旋转/孔洞回归 | 不发布候选法线，现有资产不变 |
| RISK-010-13 | 高/中 | 把法向有效率下降误报为 ACE 监督样本减少，或用同一位置多视角冒充空间覆盖 | 以 ACE 的 XYZ 1/8 掩码为监督口径；强制 8 个不同 `tile`，另报法向输入交集 | 废弃口径混淆或位置重复的报告 |
| RISK-010-14 | 低/高 | 跳过点云加载时仍有后续分支解引用 `all_pts/all_col/tree`，造成 pose-only 回归 | 领域计划统一控制四个消费者；哨兵测试 + 8+2 真跑；默认计划不变 | 恢复无条件加载 |
| RISK-010-15 | 中/中 | 把删除 LAS 邻近诊断误解为精度下降 | `las_verification` 显式标记 skipped；精度仍只用独立位姿真值，逐条零漂移验证 | 关闭 pose-only 或单独启用诊断 |
| RISK-010-16 | 低/高 | 二次投影拟合被其他逻辑隐式消费，跳过后改变位姿或质量 | 先确认调用位于最终 PnP 后；8+2 逐字段零漂移；默认路径 smoke | 恢复无条件投影验证 |
| RISK-010-17 | 中/低 | 报告把 skipped 当作投影验证失败 | 返回结构化 skipped reason，并在 benchmark 行级保存 | 恢复诊断或修正消费者 |
| RISK-010-18 | 中/高 | PnP RANSAC 的随机波动被误判为投影诊断优化造成的数值回归 | 将已有 `ransac_seed` 逐次传给求解器；同种子 A/B 生成逐字段对照证据 | 若仍不可复现，降级为统计容差并明确限制结论 |
