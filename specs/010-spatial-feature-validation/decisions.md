# 010 决策

| D-ID | 状态 | 决策 | 背景与理由 | 后果/替代方案 |
| --- | --- | --- | --- | --- |
| D-010-01 | 采用 | 本轮执行 P0/P1/P3/P4，明确跳过 P2 重模型接入 | 用户要求轻量并优先速度/准确性出结果 | MASt3R/VGGT/FreeReg 后置 |
| D-010-02 | 采用 | 查询与索引的 RGB 全局描述子保持对称；空间几何只用于候选后软评分/重排 | 原 `salad_roma.py` 多模态索引与查询 RGB-only 存在不对称风险 | 不把 XYZ/normal 零填充拼入查询召回 |
| D-010-03 | 采用 | 当前 MiDaS 法线禁止进入 PnP 硬过滤或默认 6ch 路由 | 相机系无向角中位数 36.10°–51.06°；008 还显示 6ch MiDaS 比常量恶化 23% | 下一候选为 MoGe-2-vits-normal/Metric3Dv2，门槛 ≤20° |
| D-010-04 | 采用 | 速度候选维持 `salad_v2_loftr_fast`，但不沿用“12/12=真实准确”的表述 | 12 张中 10 张 tile；真实 2 张重投影仍高 | 优先优化冷启动/KD-Tree/产物成本 |
| D-010-05 | 采用 | hloc 官方前端作为 P3 速度对照；完整 pycolmap pipeline 在 macOS 安全跳过 | 前端依赖约 82MB；pycolmap 与 Torch 双 libomp 会原生 abort | 只加载 SuperPoint/LightGlue，使用权威 NPY+OpenCV PnP |
| D-010-06 | 采用 | 完成 8+2 后仍维持默认路由，进入 hloc 轻量对照 | leave-one-out 8/8 且均值 5.57cm/0.128°，但真实查询质量门 0/2 | hloc 只复用同集，不重建 SfM |
| D-010-07 | 采用 | 离线位姿评分启用 pose-only；生产默认保留完整后处理 | warm P50 19.78s→5.41s，逐条位姿/重投影零漂移 | 冷启动仍需后续处理 5.2M 点/KD-Tree |
| D-010-08 | 采用 | 不把 hloc 升级为默认，仅保留速度对照 | 3 次 8+2 可复现；warm P50 约快 67%，但平移均值 0.1732m 是 LoFTR 的约 3.1 倍，真实质量仍 0/2 | 下一准确性候选只做候选后软评分 |
| D-010-09 | 采用 | 准确性方向先验证固定 MoGe-2 版本的 ViT-S normal，不并行安装 Metric3Dv2 | 官方权重 134.0MiB、35M 参数且直接输出 OpenCV 相机系法线；当前 MoGe 3 在 macOS 不可达 | 实跑旧参考 52.79°、修正参考 33.47°，门槛均失败，不进入软评分 |
| D-010-10 | 采用 | 暂停继续安装几何模型，先校准地图法线参考 | 已发布法线与修正四邻域候选总体相差 25.57°，逐 tile 最高 44.67°，足以污染 20° 模型资格门 | 先验证参考契约再决定 Metric3Dv2；默认定位不变 |
| D-010-11 | 采用 | 地图法线以原始 XYZ 的有效四邻域中心差分为最小可信实现，不再对 XYZ 各轴独立归一化平滑 | 旧实现合成旋转最大偏差 88°，并在孔洞/边界产生 12 个假有效法线 | 先输出 3 tile 隔离候选；发布和全量重建另行授权 |
| D-010-12 | 采用 | 保留未来预处理的法线契约修复，但不覆盖当前 3 tile 或全量发布资产 | 候选消除假法线且生成仅 0.0238s，但有效覆盖下降 6.5–10.3 个百分点，MoGe 总体仍为 33.47° | 先做 8 位置和 ACE 训练覆盖影响评估，再决定发布 |
| D-010-13 | 采用 | 停止当前法线候选路线，不发布候选、不启动 ACE 重训 | 8 位置 XYZ 监督像素 16,396 不变，但法向信息覆盖下降 13.72 个百分点；MoGe 总体 40.33° 未过 20° 门 | 准确性下一步优先补真实查询位姿真值；如重启法线路线须新规格和训练基线 |
| D-010-14 | 采用 | 正式 pose-only 直接消除稠密点云/KD-Tree加载，不实现磁盘缓存 | 5,252,140 点初始化实测 14.405s；8+2 cold 降 70.7%，位姿/质量零漂移，10/10 LAS 诊断显式 skipped | 若仍需跨进程 LAS 诊断，再另行设计带源文件指纹的持久缓存 |
| D-010-15 | 采用 | pose-only 跳过最终位姿后的二次 LightGlue 投影拟合诊断；生产默认保留 | 同种子 A/B 核心字段差异 0；相对 P4b cold -65.0%、warm P50 -86.0%，10/10 显式 skipped | 若离线任务需要像素拟合诊断，使用生产默认策略或显式控制组 |
