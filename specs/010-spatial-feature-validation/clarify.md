# 010 澄清记录

| CL-ID | 问题 | 结论或假设 | 状态 | 决策人 |
| --- | --- | --- | --- | --- |
| CL-010-01 | 用户所说 P0/P1/P3/P4 对应什么？ | P0=输出/资产可达性；P1=几何对齐与软约束可行性；P3=官方定位基线；P4=速度—准确率联合真值验证。P2 明确跳过，避免重模型。 | 已决 | 用户约束 + 本规格 |
| CL-010-02 | “准确性”能否用 PnP 重投影误差？ | 不能。重投影误差只表示同一匹配集的几何拟合；准确性必须使用 leave-one-out/独立真值的平移和旋转误差。 | 已决 | 既有统一语言 |
| CL-010-03 | 009 的 12/12 成功是否可直接沿用？ | 不可。12 张含 10 张 tile，索引可能命中自身；仅 2 张真实查询的重投影误差仍为 25.59–154.85px。 | 已证伪 | 报告数据 |
| CL-010-04 | 本轮是否安装 MoGe/hloc/pycolmap？ | 初始审计后已按用户批准安装最小 hloc 前端与固定 MoGe-2 推理依赖；均未切换默认路由，完整 pycolmap pipeline 安全跳过。 | 已更新 | 用户批准 + 轻量约束 |
| CL-010-05 | leave-one-out 1 张能否决定默认路由？ | 不能，只证明数据/位姿契约可用。默认路由至少需要 8 张留出 tile + 2 张真实查询，并通过质量门。 | 已决 | 统计下限 |
| CL-010-06 | 当前法线角误差大的原因是否已唯一定位？ | 未唯一定位。相对深度/逆深度语义是一个已验证影响因素，坐标、内参、合成域差异也可能贡献；暂不修实现。 | 开放但不阻塞设计 | 后续 P1 |
| CL-010-07 | 修复地图法线时是否覆盖已发布 `normal.npy`？ | 不覆盖。先从同一 3 张 tile 的 XYZ 生成隔离候选副本；只有契约、真实样本和门禁均通过后才提出发布动作。 | 已决 | 安全回滚约束 |
| CL-010-08 | 候选法线有效率下降是否等于 ACE 训练样本减少？ | 不等于。`SceneCoordinateDataset` 的样本准入和 `ACTLoss` 掩码只依赖 accepted、图像、XYZ 路径及 1/8 XYZ 非零；法线仅作为 6ch 输入。P1d 分别报告不变的 XYZ 监督量与变化的有效法向输入覆盖。 | 已决 | `services/localizer/ace_trainer.py` |
| CL-010-09 | 8 位置是否需要运行 ACE 重训？ | 不需要。本切片只量化现有数据契约下的输入覆盖和 MoGe 角误差；重训会显著增加成本且需单独授权和训练基线。 | 已决 | 轻量验证约束 |
| CL-010-10 | 正式 8+2 pose-only 位姿评分是否需要加载稠密 LAS/KD-Tree？ | 不需要。该配置关闭位姿先验，候选 2D–3D 对应来自 tile XYZ，PnP 和独立真值误差均不消费稠密点云；后者只服务 LAS 邻近诊断、坐标转换和视觉产物，应全部显式跳过。 | 已决 | 调用链审计 + 用户批准优化 |
| CL-010-11 | 是否为 5.2M 点新增磁盘缓存？ | 本切片不新增。实测点云/KD-Tree初始化 14.405s（进程 17.02s），而 pose-only 根本不消费它；先消除无效工作，比引入缓存失效和额外磁盘契约风险更小。 | 已决 | 最小优化原则 |
| CL-010-12 | 投影拟合诊断是否参与 PnP、质量门或独立真值评分？ | 不参与。它在最终位姿已经确定后重新运行 DISK+LightGlue，仅生成同源 tile/NPY 的像素拟合诊断；pose-only 可跳过，但生产诊断必须保留。 | 已决 | 调用顺序审计 |
| CL-010-13 | 跳过后返回 `None` 还是显式状态？ | 返回 `{"status":"skipped","reason":"pose_only_benchmark"}`，避免报告把“未运行”误解成失败或无数据。 | 已决 | 可观察性约束 |
| CL-010-14 | P4b 数值为何不能直接作为逐字段零漂移基线？ | 焦距搜索虽声明 `ransac_seed`，但此前未传入底层 OpenCV RANSAC，重复运行会产生内点/误差波动。先补齐种子传递，再用当前代码生成仅切换投影诊断的同种子 A/B；P4b 仍作为性能基线。 | 已决 | TL-010-16 Red→Green + 同种子 A/B |
