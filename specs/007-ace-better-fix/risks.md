# 007 风险与对策

| RISK-ID | 风险 | 等级 | 缓解 |
| --- | --- | --- | --- |
| RISK-007-01 | 治本 D（推理期真法线 DSINE/MiDaS + 6ch 真法线精度基准）未实施：6ch 旧模型路径（无 scene 模型环境）精度仍受「常量占位 vs 真法线」差距限制，「空间感」天花板低于真法线路径 | 中 | 明确用户已排期「最后再做 D」；本规格默认路由优先 3ch 自洽模型，常量占位仅作回退；把 D 的记录留在本 risks + decisions，建规格时引用 |
| RISK-007-02 | `ace_model_scene.pth`/`ace_model.pth` 为运行产物不入版本控制，其他环境无场景模型 → 自动落回退 6ch 路径，行为降级被误报为回归 | 中 | 路由基于存在性 + 通道检测；TL-007-02 覆盖两条分支；结果标注 `input_mode` 让降级可识别；不崩溃 |
| RISK-007-03 | 改 `solve_pnp_with_focal_search` 返回值影响 SALAD 系等调用方 | 低 | 只**新增** `attempts_summary` 键；全量回归 TL-007-07 + 门禁兜底；测试构造 6ch/3ch 两种模型路径 |
| RISK-007-04 | 前端 JS 无自动化运行时，镜像等价函数与真实 JS 行为漂移 | 低 | 沿用 005/006 先例（同名同注释 Python 镜像）；浏览器人工真验 AC-007-06 |
| RISK-007-05 | mock 测试与真实模型/真实图像差异：常量占位对真实 6ch 模型推理效果的验证无法在单测内完成 | 中 | 单测以「输入构造 / 返回值结构 / 路由选择」为断言对象（不依赖真实 3D 精度）；真实效果留给人工真验：跑一张查询图对比 PnP 成功率与 diagnostics 分布 |
| RISK-007-06 | 死代码修复（移除 `result` 引用）改变失败路径返回体，前端若依赖旧字符串结构 | 低 | 失败返回仍含 `error/tag/elapsed` 顶层键（保留兼容），仅修正内部取值来源；TL-007-05 覆盖 |