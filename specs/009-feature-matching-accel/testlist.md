# 009 测试清单

| 状态 | TL-ID | 映射 AC | 层级 | 场景与期望 |
| --- | --- | --- | --- | --- |
| [ ] | **TL-009-01** | AC-009-04 | 单元/检索 | FAISS 检索结果与 numpy 暴力余弦相似度误差 <1e-6（相同 query，相同索引） |
| [ ] | **TL-009-02** | AC-009-04 | 单元/降级 | 无 FAISS 时回退 numpy，结果一致；无 XFeat 时该算法不注册 |
| [ ] | **TL-009-03** | AC-009-02 | 单元/compile | torch.compile(mps) 成功或回退 eager 不报错；输出 shape 正确 |
| [ ] | **TL-009-04** | AC-009-03 | 单元/匹配 | fast_mode 输出匹配点数 ≥ 原模式的 80%（精度不显著下降） |
| [ ] | **TL-009-05** | AC-009-01 | 单元/registry | 新算法 ids 可用；原算法代码 diff 为空（不修改） |
| [ ] | **TL-009-06** | AC-009-05 | 单元/前端 | 前端新 key 存在且 label 含 [加速]/[XFeat]；原 key 文案不变 |
| [ ] | **TL-009-07** | AC-009-02 | 集成/benchmark | fast_loftr 延迟 ≤ 原 loftr × 1.3（同硬件） |
| [ ] | **TL-009-08** | AC-009-03 | 集成/benchmark | fast_loftr 成功率 ≥ 原 loftr（同查询集） |
| [ ] | **TL-009-09** | AC-009-06 | 回归+门禁 | validate-specs / run-all fast / drift-check / 全量 pytest 全绿 |

## TDD 顺序

### 批次 P1（首选）
1. TL-009-01 红 → 绿（FAISS 等价 + 降级）
2. TL-009-03 红 → 绿（compile 成功/回退）
3. TL-009-04 红 → 绿（fast_mode 精度）
4. TL-009-05 红 → 绿（注册 + 原算法不动）

### 批次 P2（XFeat，与 P1 并行）
5. TL-009-02 覆盖（XFeat 未安装不注册）
6. TL-009-06 前端新 key

### 批次 P3（批量异步，与 P1/P2 并行）
7. 批量等价单测

### 批次 P4（前端）
8. TL-009-06 前端验证

### 批次 P5（基准+门禁）
9. TL-009-07/08/09
