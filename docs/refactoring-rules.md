# 重构规则

1. Red 阶段只加入一个失败行为；先确认失败原因与预期一致。
2. Green 阶段只实现足够通过当前测试的改动，不同时进行大范围目录迁移。
3. 全部相关测试变绿后才进入 Refactor。
4. 重构不得改变外部可观察行为；若必须改变，先更新规格与测试清单。
5. 涉及坐标系、任务状态、持久化 schema、地图资产格式或算法结果结构的重构，必须有契约或集成测试。
6. 跨限界上下文搬移代码需单独规格包，包含依赖图、迁移顺序和回滚方案。
7. 删除兼容入口前先证明没有调用方，并在 `decisions.md` 记录决定。
